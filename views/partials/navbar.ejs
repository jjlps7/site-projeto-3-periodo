<div class="header-container">
    <h1 class="logo">
        Base
        <span>5</span>
        automações
    </h1>
    <nav class="nav" aria-label="Navegação principal">
        <a href="/">Início</a>
        <a href="/sobre">Sobre</a>
        <a href="/perfil">Perfil</a>
        <a href="/projetos">Projetos</a>
        <% if (usuario) { %>
        <a href="/pedidos"><%= usuario.is_admin ? 'Gerenciar Pedidos' : 'Meus Pedidos' %></a>
        <% } %>
        <% if (usuario) { %>
        <span style="color: #4d8fff; font-weight: bold; margin-left: 20px">
            Olá, <%= usuario.nome %>
        </span>
        <a href="/logout" style="color: #c0392b">Sair</a>
        <% } else { %>
        <a href="/login" style="margin-left: 20px">Login</a>
        <% } %>
    </nav>
</div>
